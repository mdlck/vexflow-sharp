---
phase: quick-260328-cv5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "README reflects the actual project structure (10 projects, not 3)"
    - "Test count matches reality (546 passing, 8 skipped, 554 total)"
    - "Line count matches reality (~27,000+ lines across all projects)"
    - "VexFlowSharp.Skia library and per-platform packages are documented"
    - "Unity samples section lists both GrandStaffDemo and ComplexNotationDemo"
    - "Build instructions cover the multi-project solution correctly"
  artifacts:
    - path: "README.md"
      provides: "Complete, accurate project documentation"
---

<objective>
Update README.md to accurately reflect the current state of the VexFlowSharp project after v1.0 completion.

Purpose: The current README has stale numbers (537 tests, ~25,500 LOC), describes only 3 libraries (missing VexFlowSharp.Skia and 6 platform-specific Skia packages), and does not mention the ComplexNotationDemo Unity sample. A developer or GitHub visitor should see accurate, comprehensive documentation.

Output: Updated README.md
</objective>

<execution_context>
@/home/vboxuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/vboxuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with accurate project information</name>
  <files>README.md</files>
  <action>
Update README.md with the following specific changes. Preserve the existing structure, tone, and code examples (they are correct). Only modify sections that contain stale or incomplete information.

**1. Overview section — fix numbers:**
- Change "537 NUnit tests passing" to "546 NUnit tests passing (554 total, 8 skipped)"
- Change "approximately 25,500 lines of C#" to "approximately 27,000 lines of C#"

**2. Project Structure section — expand from 3 to full library set:**

Replace the 3-row table with a comprehensive table covering all projects:

| Library | Target | Purpose |
|---------|--------|---------|
| `VexFlowSharp.Common` | `netstandard2.1` | Core library — all notation classes, formatting engine, high-level API (`Factory`, `EasyScore`, `System`), and rendering abstractions. No platform-specific dependencies. |
| `VexFlowSharp.Skia` | `net10.0` | SkiaSharp-based `SkiaRenderContext` and `ImageComparison` utility. Shared rendering backend for tests and standalone .NET applications. |
| `VexFlowSharp.Skia.Linux` | `net10.0` | Platform package bundling `SkiaSharp.NativeAssets.Linux` for Linux deployment. |
| `VexFlowSharp.Skia.Windows` | `net10.0` | Platform package bundling `SkiaSharp.NativeAssets.Win32` for Windows deployment. |
| `VexFlowSharp.Skia.macOS` | `net10.0` | Platform package bundling `SkiaSharp.NativeAssets.macOS` for macOS deployment. |
| `VexFlowSharp.Skia.iOS` | `net10.0` | Platform package bundling SkiaSharp native assets for iOS deployment. |
| `VexFlowSharp.Skia.Android` | `net10.0` | Platform package bundling SkiaSharp native assets for Android deployment. |
| `VexFlowSharp.Skia.WebAssembly` | `net10.0` | Platform package bundling SkiaSharp native assets for Blazor/WASM deployment. |
| `VexFlowSharp.Tests` | `net10.0` | NUnit test suite with pixel-comparison testing against VexFlow JS reference images. |
| `VexFlowSharp.Unity` | Unity UPM package | `VexFlowElement` (custom `VisualElement`) and `UIElementsRenderContext` for Unity UI Toolkit / Painter2D rendering. Requires Unity 2022.1+. |

After the table, add a brief explanation: "The `VexFlowSharp.Skia.*` platform packages exist because SkiaSharp requires platform-specific native assets. Reference the appropriate platform package for your target OS. For cross-platform builds, reference `VexFlowSharp.Skia` directly and add SkiaSharp native asset packages manually."

**3. Quick Start (.NET / Console) section — update SkiaRenderContext source:**
Change "The `SkiaRenderContext` is defined in `VexFlowSharp.Tests`." to "The `SkiaRenderContext` is defined in `VexFlowSharp.Skia`."

**4. Add a "Unity Samples" section after the Grand Staff Example section:**

Add a new H2 section:

```
## Unity Samples

The `VexFlowSharp.Unity` package includes two ready-to-use samples in the `Samples~` folder:

| Sample | Description |
|--------|-------------|
| **GrandStaffDemo** | Renders a piano grand staff (treble + bass) with brace connector. Demonstrates `VexFlowElement`, `Factory`, `EasyScore`, and `System` usage. |
| **ComplexNotationDemo** | Renders a multi-measure score with accidentals, articulations, beams, ties, tuplets, dynamics, and other modifiers. Demonstrates the full range of VexFlowSharp notation features. |

Each sample includes a controller script, a Unity scene, and setup instructions. Import them via Unity Package Manager: select the VexFlowSharp.Unity package, expand Samples, and click Import.
```

**5. Building section — update test count:**
Change "Run the 537 NUnit tests" comment to "Run the test suite (546 passing)"

**6. Adding to a .NET Project subsection — mention Skia packages:**
After "Reference `VexFlowSharp.Common` as a project reference or NuGet package.", update the rendering backend guidance: "For SkiaSharp-based rendering, also reference the appropriate `VexFlowSharp.Skia.*` platform package for your target OS (e.g., `VexFlowSharp.Skia.Linux` for Linux). The `SkiaRenderContext` class in `VexFlowSharp.Skia` provides a ready-to-use rendering backend."

Do NOT change: code examples, Key Differences table, Font Setup, Draw Order, What Is Implemented, Known Limitations, or License sections. These are all accurate.
  </action>
  <verify>
    <automated>grep -c "546" README.md && grep -c "VexFlowSharp.Skia" README.md && grep -c "ComplexNotationDemo" README.md</automated>
  </verify>
  <done>README.md contains: accurate test count (546), all 10 projects documented in the structure table, VexFlowSharp.Skia mentioned as the SkiaRenderContext source, both Unity samples listed, updated .NET project setup guidance referencing Skia platform packages.</done>
</task>

</tasks>

<verification>
- README.md has no references to "537" (old test count)
- README.md mentions all Skia platform variants (Linux, Windows, macOS, iOS, Android, WebAssembly)
- README.md mentions both Unity samples (GrandStaffDemo, ComplexNotationDemo)
- All existing code examples are unchanged
</verification>

<success_criteria>
A developer visiting the GitHub repo sees an accurate, comprehensive README reflecting the full v1.0 project with all libraries, correct metrics, and both Unity samples documented.
</success_criteria>

<output>
After completion, create `.planning/quick/260328-cv5-update-readme-md-to-reflect-all-current-/260328-cv5-SUMMARY.md`
</output>
